# Tantan 实施落地方案

> 状态：已批准，可进入实施  
> 目标：在 Folo 前端基础上，交付 PC Web 与 Mobile Web/PWA 共用的一套响应式产品；本地 Go 服务代理 Folo 数据与认证，并独立提供推荐、Topic、搜索、翻译、摘要和 AI 筛选。  
> 详细规格：[前端实施规格](./2026-08-09-tantan-frontend-spec.md) · [后端实施规格](./2026-08-09-tantan-backend-spec.md)
> Agent 执行入口：[`spec-package/README.md`](./spec-package/README.md)
> 批准记录：2026-08-09，用户要求继续直至产出可落地、安全、Agent 可编码的 spec package；一期执行边界因此锁定为 PC Web + 响应式 Mobile Web/PWA。

## 1. 已确定结论

1. Folo 仅作为账号、订阅、Feed/Entry、已读、收藏和添加订阅的数据源。
2. 所有 Folo 会员、付费、Power、Wallet、Plan、Upgrade、付费 AI 与 AI Chat 产品代码退出最终产物。
3. 前端不得调用 Folo `/ai/**`、`/wallets/**`、付费订阅或支付接口；Go 代理以路由白名单和拒绝规则双重保证。
4. 用户自己的 API Key 只保存在本机系统钥匙串，只由本地 Go 服务读取；浏览器、日志、Folo 请求均不得包含 Key。
5. Topic、推荐、AI Filter、翻译、摘要和中文搜索索引由本地 Go 服务实现。
6. 首页使用最近 7 天未读内容建立每日队列：初始 50 条、当日最多 60 条；更早内容仍存在于订阅页、Source Detail、收藏和搜索。
7. 一期不部署服务器。Go 仅监听 `127.0.0.1:3000`，前端开发端口为 `127.0.0.1:5173`。
8. 前端锁定 Folo `dev` 分支提交 `3846c90b67da351b6017cd4fe9d0992b8077224e`，客户端 SDK 锁定 `@follow-app/client-sdk@0.3.95`。

## 2. 一期端形态决定

一期 Mobile 明确指 Mobile Web/PWA：PC 与 Mobile 共用 `apps/desktop/layer/renderer`、路由、状态和接口；`<768px` 使用底部三栏。iOS/Android 原生 App 不在一期范围；若未来纳入，必须新增针对 `apps/mobile` React Native 工程的实施规格，不得直接扩展本任务树。

## 3. 目标架构

```text
┌─────────────────────────────────────────────────────────┐
│ React 19 Web/PWA                                        │
│ PC：Folo 桌面信息密度 + 左侧一级导航                    │
│ Mobile：小红书双列瀑布流 + 首页/订阅/设置底栏           │
└─────────────────────────┬───────────────────────────────┘
                          │ HTTP/JSON，同源或受限开发 CORS
┌─────────────────────────▼───────────────────────────────┐
│ Go 本地服务 127.0.0.1:3000                              │
│                                                         │
│ 认证桥接 │ Folo 兼容代理 │ 同步/缓存 │ 推荐/Topic       │
│ AI Provider │ 翻译/摘要 │ 搜索 FTS5 │ 设置/反馈         │
└──────────────┬─────────────────────────┬────────────────┘
               │                         │
      HTTPS 固定上游                本地持久化
               │                         │
┌──────────────▼──────────┐    ┌────────▼─────────────────┐
│ api.folo.is/app.folo.is │    │ SQLite + OS Keychain     │
│ 账号/订阅/Entry/Read    │    │ 缓存/队列/Topic/AI Key   │
└─────────────────────────┘    └──────────────────────────┘
```

数据所有权必须保持单一：

| 数据                               | 唯一事实源       | 本地角色               |
| ---------------------------------- | ---------------- | ---------------------- |
| Folo 用户、订阅、Entry、已读、收藏 | Folo             | 兼容代理与可重建缓存   |
| AI Provider 配置                   | Go + OS Keychain | 本机事实源             |
| 翻译、摘要、质量分、Topic          | Go + SQLite      | 本机事实源，可重新生成 |
| 推荐反馈、屏蔽、频道排序           | Go + SQLite      | 本机事实源             |
| 每日推荐队列                       | Go + SQLite      | 当日稳定快照，可重建   |

## 4. 仓库落点

实施时把 `/Users/mingrui/Project/tantan` 建成基于 Folo 历史的单仓库，不在 `/Users/mingrui/Project/Folo` 直接修改：

```text
/Users/mingrui/Project/tantan/
├── apps/desktop/...                 # Folo Web 前端的保留与魔改部分
├── packages/internal/...            # Folo Store/DB/组件的保留与适配部分
├── services/tantan-api/
│   ├── cmd/tantan-api/main.go
│   ├── internal/auth/
│   ├── internal/folo/
│   ├── internal/home/
│   ├── internal/ai/
│   ├── internal/search/
│   ├── internal/storage/
│   ├── migrations/
│   └── go.mod
├── docs/
│   ├── prd.md
│   └── prototype.zip
├── 2026-08-09-tantan-实施落地方案.md
├── 2026-08-09-tantan-frontend-spec.md
└── 2026-08-09-tantan-backend-spec.md
```

导入基线使用以下非破坏步骤；执行前先提交或备份当前两个输入文件：

```bash
cd /Users/mingrui/Project/tantan
git init
git remote add folo-upstream /Users/mingrui/Project/Folo
git fetch folo-upstream
git switch -c main 3846c90b67da351b6017cd4fe9d0992b8077224e
mkdir -p docs
mv 'prd(5).md' docs/prd.md
mv 'tantan前端原型.zip' docs/prototype.zip
git add docs
git commit -m 'docs: add tantan product inputs'
```

如果 checkout 因未跟踪文件冲突而失败，停止执行并保留现状；不得使用 `git clean`、`git reset --hard` 或覆盖输入文件。

## 5. PC 与 Mobile 的统一方式

### PC Web（`>=768px`）

- 保留 Folo 桌面端的信息密度和左侧导航结构，但一级入口收敛为：首页、订阅、设置。
- 首页内容区不再使用传统 Timeline，替换为 2～4 列 Masonry：
  - `768–1023px`：2 列；
  - `1024–1439px`：3 列；
  - `>=1440px`：4 列。
- Article/Post Detail 复用 Folo 正文渲染器；翻译和摘要数据源替换为本地 Go。

### Mobile Web/PWA（`<768px`）

- 替换当前 Folo `DownloadPage` 分支，进入真实应用。
- 固定底栏：首页、订阅、设置。
- 首页始终双列；Header、Topic、AI Filter 状态栏保持 sticky。
- 详情页全屏推进，浏览器返回键恢复原 Topic、滚动位置和队列游标。
- 触控菜单使用 Bottom Sheet；PC 使用 Popover/Context Menu，操作语义一致。

### 原型的使用边界

`tantan前端原型.zip` 仅作为视觉和交互证据。禁止直接复制其单文件 `App.tsx` 架构、静态数据或整套 MUI/Radix 依赖；Masonry 使用 Folo 已有的 `masonic@4.1.0`，状态继续使用 Folo 已有 Zustand、Jotai 和 React Query。

## 6. Folo 兼容与硬隔离

Go 服务对 Folo SDK 保留原始路径、状态码和 JSON 结构。允许的核心前缀：

```text
/better-auth/get-session
/better-auth/one-time-token/apply
/better-auth/sign-out
/entries/**
/subscriptions/**
/reads/**
/collections/**
/feeds/**
/categories/**
/discover/**
/profiles/**
/lists/**
/inboxes/**
/settings/**
```

以下路径必须返回 `410 FOLO_FEATURE_REMOVED`，不得转发：

```text
/ai/**
/wallets/**
/better-auth/subscription/**
/better-auth/stripe/**
/payments/**
/referrals/**
/trending/**
/rsshub/use
```

未知路径默认 `403 FOLO_ROUTE_NOT_ALLOWED`。添加新的上游路径必须同时补充合同测试、用途和数据分类。

## 7. 本地认证

Google 登录不复制 Folo OAuth 服务端：

1. 浏览器访问 `GET /auth/folo/start`。
2. Go 生成一次性 `state`，设置 HttpOnly Cookie。
3. Go 跳转到 `https://app.folo.is/login?cli_callback=http://127.0.0.1:3000/auth/folo/callback?...`。
4. Folo 完成 Google 登录并把 one-time token 返回 loopback callback。
5. Go 校验 `state`，调用上游 `/better-auth/one-time-token/apply`。
6. 上游 session token 存入 OS Keychain；浏览器只得到随机 `tantan_session` HttpOnly Cookie。
7. 后续代理请求由 Go 注入上游 session token。

本方案不在本地重新实现 Google OAuth，不存储 Google 凭据，不把上游 token 放进 localStorage。

## 8. 首页与 AI 的确定规则

### 每日队列

- 时区来自浏览器 IANA timezone；缺失时拒绝生成并要求前端补传，不用服务端时区猜测。
- 候选：最近 7×24 小时、仍未读、来自当前账号订阅的 Entry，最多取最新 500 条参与排序。
- 初始队列 50 条；当天新内容按排序分追加，硬上限 60 条。
- 进入详情并成功标记已读后，该 Entry 从所有首页 Topic 视图退出。
- 队列全部读完显示“今天值得看的内容已经看完 ✓”。
- Topic 队列读完显示“今天的 {Topic} 内容已经看完 ✓”。

### 排序

基础分由以下确定项相加：

```text
recency         0..40
topic_affinity  0..20
source_affinity 0..15
quality         0..15
filter_match    0..30（仅 Active Filter）
```

- 已读、屏蔽 Source、明确不感兴趣：直接排除。
- 首页前 20 条中同 Source 最多 3 条、同主 Topic 最多 5 条、相同 Source 不得连续超过 2 条。
- 缺少 AI 质量分时按 `quality=5` 排序，AI 不可用不得阻止首页展示。

### AI Provider

- 一期提供 OpenAI、Anthropic、Google、DeepSeek、OpenRouter 内置 Provider：用户只选 `providerId`、`model`并填 `apiKey`，endpoint 不可编辑。
- 支持连接测试；Key 只进 OS Keychain，响应永不回传 Key。
- AI 返回必须符合 JSON Schema；失败后仅允许一次修复请求。
- 429/5xx 最多重试 2 次，单次超时 60 秒，并发 2。
- 翻译、摘要或分类失败时保留原文并显示非阻塞状态。

### 首页组件与四条交互链路

```text
HomePage（唯一首页查询状态：activeTopicId、activeFilterId、各 Topic scrollY）
├── HomeHeader（普通搜索、AI 智能筛选两个图标）
├── TopicTabs（稳定 topicId，不用名称查询）
├── ActiveAIFilterBar（显示、编辑、重置当前筛选）
├── MasonryFeed（masonic 虚拟瀑布流）
└── AIFilterSheet（自然语言生成首页）
```

1. 瀑布流只分页读取 Go 已生成的稳定队列：`GET /tantan/v1/home?topicId=...&filterId=...&cursor=...&limit=20`。React Query Key 固定为 `['home', topicId, filterId]`；前端按 `entryId` 再去重，禁止滚动时重新调用 AI 排序。
2. Topic 点击先保存旧 Topic 的 `scrollY`，再切换 `activeTopicId`。首次进入滚到顶部；切回已有 Topic 时复用 Query Cache 并在布局完成后恢复该 Topic 的滚动位置。
3. 普通搜索图标导航到 `/search`，只查询本地全文索引；它不修改 `activeTopicId`、`activeFilterId` 或每日队列，返回时恢复首页状态。
4. AI 图标只打开 `AIFilterSheet`，不跳路由。提交 `PUT /tantan/v1/filter` 后，Go 完成 `Prompt→FilterSpec→JSON 校验→动态 Topic→新 Queue`；前端切回 `recommend`、刷新 Topic/Home、关闭 Sheet 并滚到顶部。重置调用 `DELETE /tantan/v1/filter`。

`HomeResponse` 返回 `items,nextCursor,queue`；游标绑定 `queueId+version+topicId+filterId+afterRank`。打开详情并成功标记已读后，前端立即从所有 `['home', ...]` 缓存页删除该 `entryId`；图片加载失败改成文字卡，不显示破图占位。

## 9. 交付阶段

| 阶段                  | 结果                                               | 退出条件                                             |
| --------------------- | -------------------------------------------------- | ---------------------------------------------------- |
| P0 基线与合同         | Tantan 仓库、Folo 基线、SDK 路由清单、现有测试结果 | Folo Web 能启动；输入文件安全保留；合同夹具完成脱敏  |
| P1 Go 代理与认证      | 本地 Google 登录、核心 Folo 数据通过 Go            | 浏览器无直连 `api.folo.is`；登录后恢复订阅/已读/收藏 |
| P2 去付费与去 Folo AI | 所有会员/钱包/升级/付费 AI UI、路由和请求消失      | 静态扫描和 Playwright 均证明零付费入口、零被禁请求   |
| P3 响应式 Shell       | PC 左侧导航、Mobile 底栏、Mobile 不再下载页        | 360px 与 1440px 完成三大入口 E2E                     |
| P4 核心内容页         | 首页瀑布流、详情、订阅、收藏、Source Detail、搜索  | 真实 Folo 账号完成核心旅程                           |
| P5 本地 AI            | Provider 设置、翻译、摘要、Topic、AI Filter        | Key 不泄漏；模型失败时原文和默认 Feed 可用           |
| P6 推荐闭环           | 每日队列、已读退出、负反馈、频道管理               | 队列稳定、可看完、重启后状态一致                     |
| P7 收口               | PWA、性能、无障碍、备份恢复、文档                  | 全量测试、构建、人工验收通过                         |

详细 TDD 任务、文件写入范围、API、Schema 和测试命令位于前后端规格。

## 10. 本地启动目标

```bash
# Terminal 1：Go
cd /Users/mingrui/Project/tantan/services/tantan-api
go run ./cmd/tantan-api

# Terminal 2：Web
cd /Users/mingrui/Project/tantan
VITE_API_URL=http://127.0.0.1:3000 \
VITE_WEB_URL=http://127.0.0.1:5173 \
pnpm --dir apps/desktop dev:web
```

健康检查：

```bash
curl --fail http://127.0.0.1:3000/healthz
curl --fail http://127.0.0.1:3000/readyz
```

成功结果分别为 `{"status":"ok"}` 与包含 `sqlite:"ok"`、`keyring:"ok"` 的 JSON。

## 11. 验收总线

- PC Chromium 1440×900 与 Mobile Chromium 390×844 均通过核心 E2E。
- 首页首屏在 500 条本地候选数据下 2 秒内出现 Skeleton 后内容；滚动保持 50fps 以上。
- 所有上游请求只到固定 `https://api.folo.is`，且只能由 Go 发起。
- 浏览器存储、请求、日志、错误信息中不存在 API Key 和 Folo session token。
- Folo `/ai/**`、`/wallets/**`、付费订阅和支付接口的调用数为 0。
- 模型、Folo 或网络任一依赖失败时，已有内容仍可阅读；错误状态提供重试入口。
- 今日队列消费完成后出现完成态；历史内容仍可通过订阅、Source Detail、收藏和搜索访问。
- `pnpm typecheck`、前端 Vitest、Playwright、`go test ./...`、Go race test、构建与规格校验全部通过。

## 12. 风险和边界

1. Folo 后端并非本项目所有；SDK 或接口变更只能通过锁版本、合同夹具和兼容代理控制，无法承诺上游长期稳定。
2. Folo 源码使用 AGPL-3.0，`icons/mgc` 另有禁止再分发例外。本期仅本地运行；任何公开部署、分发或商业发布前，必须完成许可证审查、源码提供机制、品牌替换和图标替换。
3. PWA 依赖本机 Go 服务；Go 未运行时只展示缓存内容和“本地服务未启动”，不能执行同步或 AI。
4. 首次登录需要后台建立完整搜索索引；建立期间先支持最近已同步内容，并显示明确进度。

## 13. 批准与变更方式

本方案已作为一期实施基线。任何改变公共 API、安全边界、持久化模型、Folo 路由白名单或一期端形态的需求，必须先修订规格包并重跑最终校验，Agent 不得在实现中自行扩大范围。

> **已被替代（2026-08-10）**：本文的 PC Web、Electron、本地 loopback 和浏览器配置 AI Key 方案不再实施。当前唯一可编码规格为 `spec-package/` v2：只交付 Mobile Web/PWA；Folo Mobile 为非首页基线；Go 提供同源 `/api`、全部 Folo 登录方式和服务端 Gemini 配置。
